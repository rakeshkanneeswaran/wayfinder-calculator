/**
 * Placeholder. The real Keypad renders the 18-key grid from a static layout
 * config and dispatches actions; that arrives with the calculator core slice.
 */
export function Keypad() {
  return <div role="group" aria-label="Keypad" className="grid min-h-64 grid-cols-4 gap-2" />;
}
